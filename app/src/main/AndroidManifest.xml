<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fire_information_apparatus">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fire_information_apparatus">
        <activity android:name=".Statics" android:theme="@android:style/Theme.Dialog"></activity>
        <activity android:name=".Detail_Edit_Activity" />
        <activity android:name=".Add_Object_Activity" />
        <activity android:name=".Edit_Child_Activity" />
        <activity android:name=".Detail_activity" />
        <activity android:name=".Add_ListView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>